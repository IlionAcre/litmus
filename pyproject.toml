[project]
name = "litmus"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.4",
    "fastapi>=0.139.2",
    "litellm<1.90",
    "numpy>=2.5.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "scipy>=1.18.0",
    "typer>=0.27.0",
    "uvicorn>=0.51.0",
]

[project.scripts]
litmus = "litmus.cli:app"

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

# [tool.litmus]
# Uncomment and adjust any of these to override Litmus's defaults for this
# project. CLI flags, where available, still override this file; this file
# overrides the hardcoded defaults shown below.
# runs_dir = "runs"
# log_file = "logs/litmus.jsonl"
# log_level = "INFO"
# log_max_bytes = 5000000
# log_backup_count = 3
# host = "127.0.0.1"
# port = 8000
# alpha = 0.05
# confidence = 0.95
# min_case_count = 10
# min_discordant_pairs = 10
# exact_threshold = 25
# n_resamples = 10000
# max_workers = 4
#
# [tool.litmus.scorers.semantic_similarity]
# model = "gemini/text-embedding-004"
# threshold = 0.8
#
# [tool.litmus.scorers.llm_judge]
# model = "gemini/gemini-2.5-flash-lite"
# threshold = 0.5
